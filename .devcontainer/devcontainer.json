{
  "name": "Django 6 Dev Container",
  "image": "mcr.microsoft.com/devcontainers/python:3",
  "features": {},
  "postCreateCommand": "curl -LsSf https://astral.sh/uv/0.9.22/install.sh | sh && source $HOME/.cargo/env && uv sync && source .venv/bin/activate && pipx install pre-commit && pre-commit install --install-hooks",
  "remoteEnv": {
    "PATH": "${containerEnv:PATH}:/home/vscode/.cargo/bin"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  }
}
