{
  "coreTools": [
    "FileEditor",
    "ShellTool",
    "FindFiles"
  ],
  "excludeTools": [
    "ShellTool(rm -rf)",
    "ShellTool(shutdown)",
    "ShellTool(gh)",
    "ShellTool(git push)"
  ]
}
